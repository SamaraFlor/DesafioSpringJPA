


spring:
  datasource:
    url: jdbc:Mysql://localhost:3306/academia_db?useTimezone=true&serverTimezone=UTC&useLegacyDate
    username: root
    password: 1234
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create
    properties:
      hibernate.format_sql: true

logging:
  level:
    org:
      hibernate:
        type: trace
